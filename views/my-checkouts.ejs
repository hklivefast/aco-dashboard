<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Checkouts - LIVEFAST ACO</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar">
    <a href="/" class="navbar-brand">
      <span>AC</span> LIVEFAST ACO
    </a>
    
    <div class="navbar-nav">
      <a href="/dashboard">Dashboard</a>
      <a href="/products">Select Products</a>
      <a href="/my-selections">My Selections</a>
      <a href="/releases">Releases</a>
      <a href="/my-checkouts" class="active">My Checkouts</a>
      
      <% if (user.isAdmin) { %>
      <a href="/admin">Admin</a>
      <% } %>
      
      <div class="user-info">
        <% if (user.avatar) { %>
        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="<%= user.username %>">
        <% } else { %>
        <img src="https://cdn.discordapp.com/embed/avatars/0.png" alt="<%= user.username %>">
        <% } %>
        <span><%= user.username %></span>
        <a href="/logout" class="btn btn-secondary btn-sm">Logout</a>
      </div>
    </div>
  </nav>

  <main class="container">
    <div class="hero" style="padding: 2rem;">
      <h1 style="font-size: 2rem;">My Checkouts</h1>
      <p>Track your automated checkout results</p>
    </div>

    <div class="card">
      <div class="card-header">
        <h2 class="card-title">Checkout History</h2>
        <span class="badge badge-info"><%= checkouts.length %> Checkouts</span>
      </div>
      
      <% if (checkouts && checkouts.length > 0) { %>
        <div style="display: flex; flex-direction: column; gap: 0.75rem;">
          <% checkouts.forEach(function(checkout) { %>
          <div class="checkout-item-card">
            <img src="<%= checkout.product_image || '/images/placeholder.png' %>" alt="<%= checkout.product_name || 'Product' %>" class="checkout-item-image" onerror="this.style.display='none'">
            <div class="checkout-item-details">
              <div class="checkout-item-name">
                <%= checkout.product_name || checkout.sku || 'Unknown Product' %>
              </div>
              <div class="checkout-item-meta">
                Qty: <%= checkout.quantity %> | 
                <span class="badge badge-<%= checkout.status === 'success' ? 'success' : (checkout.status === 'pending' ? 'warning' : 'danger') %>">
                  <%= checkout.status %>
                </span>
                <% if (checkout.price) { %> | $<%= checkout.price %><% } %>
              </div>
            </div>
            <div style="text-align: right; font-size: 0.8125rem; color: var(--text-muted);">
              <%= new Date(checkout.created_at).toLocaleString() %>
            </div>
          </div>
          <% }); %>
        </div>
      <% } else { %>
      <div class="empty-state">
        <div class="empty-state-icon">BOX</div>
        <p>No checkouts yet.</p>
        <p style="font-size: 0.875rem; margin-top: 0.5rem;">
          Checkouts will appear here when your Discord bot sends webhook notifications.
        </p>
      </div>
      <% } %>
    </div>

    <div class="card mt-3">
      <div class="card-header">
        <h2 class="card-title">Webhook Integration</h2>
      </div>
      <div style="color: var(--text-secondary);">
        <p>To receive checkout notifications, configure your Discord bot to send webhooks to:</p>
        <code style="display: block; background: var(--bg-tertiary); padding: 1rem; border-radius: var(--border-radius); margin: 1rem 0;">
          POST https://aco-dashboard-production.up.railway.app/webhooks/checkout
        </code>
        <p style="font-size: 0.875rem;">Expected payload format will be documented soon.</p>
      </div>
    </div>
  </main>

  <footer class="footer">
    <p>LIVEFAST ACO - Automated Checkout Services</p>
  </footer>

  <script src="/js/app.js"></script>
</body>
</html>
